version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/
    overwrite: true
permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: apache

hooks:
  BeforeInstall:
  AfterInstall:
  ApplicationStop:
